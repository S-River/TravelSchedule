<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.nkc_ct02.travelschedule">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_test"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_test_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RyokouSakuei"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".MotimonoGamen"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".HiyoukanriGamen"
            android:windowSoftInputMode="adjustPan"/>
    </application>

</manifest>